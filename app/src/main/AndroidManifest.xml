<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Permisiune pentru notificări (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Permisiuni pentru background -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- PERMISIUNI PENTRU SISTEMUL DE UPDATE -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.DOWNLOAD_WITHOUT_NOTIFICATION" />

    <!-- Permisiuni storage pentru Android 9 și mai jos -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <uses-feature
        android:name="android.hardware.biometrics.face"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.fingerprint"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/jellyseerricon"
        android:label="Jellyseerr"
        android:roundIcon="@mipmap/jellyseerricon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Jellyseerr"
        android:requestLegacyExternalStorage="true"> <!-- IMPORTANT pentru Android 10+ -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Jellyseerr"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask"> <!-- Adaugă acest lucru -->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ UN SINGUR SERVICIU FIREBASE CORECT CONFIGURAT -->
        <service
            android:name=".FirebaseNotificationService"
            android:exported="false"
            android:stopWithTask="false"> <!-- ACESTA PERMITE RULAREA ÎN BACKGROUND -->
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Provider pentru FileProvider (Android 7+) pentru update -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Download Service pentru update -->
        <service
            android:name="android.app.DownloadService"
            android:exported="false" />

        <!-- Alte activități/service-uri dacă ai -->

    </application>

</manifest>